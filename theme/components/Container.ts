import type { ThemeComponents } from '@chakra-ui/react'

const Container: ThemeComponents['Container'] = {
  baseStyle: {
    paddingLeft: [4, null, 6, 10, 12],
    paddingRight: [4, null, 6, 10, 12],
    maxW: 'container.xl',
  },
}

export default Container
